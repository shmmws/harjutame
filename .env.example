# SFTP Deployment Configuration
# Copy this file to $HOME/configs/.env and fill in your values
# Or set SFTP_ENV_DIR to use a different location

# Required: SFTP host address
SFTP_HOST=your-server.com

# Required: SFTP username
SFTP_USER=your_username

# Optional: SFTP port (default: 22)
SFTP_PORT=22

# Optional: Remote directory path on server (default: current directory)
SFTP_REMOTE_DIR=/var/www/html/harjutame

# Optional: Path to SSH private key (default: $ENV_DIR/smws)
SSH_KEY_PATH=~/.ssh/id_rsa

# Optional: Passphrase for SSH key (avoid if possible; use ssh-agent instead)
# KEY_PASSPHRASE=your_key_passphrase

# Optional: Custom log file location (default: ./deploy_sftp.log)
# DEPLOY_LOG=./deploy_sftp.log
